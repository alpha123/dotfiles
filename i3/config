# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod2 control

# Sometimes Return works, sometimes KP_Enter.
set $return Return

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# When font size is 0, no text is drawn and small (~5px) bars are shown.
font pango:Muli 0

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Start a terminal
# Assume urxvtd was started in xinitrc
bindsym $mod+$return exec "urxvtc -e fish"

# Start a web browser
bindsym $mod+Shift+$return exec dwb

# Start dmenu (a program launcher)
bindsym $mod+space exec ~/dmenu-4.5/dmenu_launcher

# Kill focused window
bindsym $mod+q kill

# Jump between windows quickly
bindsym $mod+Tab exec "rofi -now -font 'Muli 10' -fg '#505050' -bg '#00000' -hlfg '#ffffff' -hlbg '#000000' -o 85"

bindsym $mod+m exec "~/scripts/dmenu_i3_mark"
bindsym $mod+g exec "~/scripts/dmenu_i3_goto"
# 'grave' is the backtick
bindsym $mod+grave exec "~/scripts/dmenu_i3_goto"


# Change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Split in horizontal orientation
bindsym $mod+b split h

# Split in vertical orientation
bindsym $mod+v split v

# Enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# Send the current window to the scratchpad workspace
bindsym $mod+Shift+p move scratchpad

# Show the first stratchpad window
bindsym $mod+p scratchpad show

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# Toggle tiling / floating
bindsym $mod+Shift+w floating toggle

# Change focus between tiling / floating windows
bindsym $mod+w focus mode_toggle

# Focus the parent container
bindsym $mod+a focus parent

# Focus the child container
bindsym $mod+z focus child

# Switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
bindsym $mod+BackSpace workspace back_and_forth
bindsym $mod+minus exec "~/scripts/dmenu_i3_workspace"

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10
bindsym $mod+Shift+BackSpace move container to workspace back_and_forth
bindsym $mod+Shift+minus exec "~/scripts/dmenu_i3_move_to_workspace"

# Some personal preferences
hide_edge_borders both
focus_follows_mouse no

new_window pixel 0
# Hide the title bars of certain windows
#for_window [title="Chromium"] border none
#for_window [title="Firefox"] border none
# Hide title bars for all windows
for_window [class="^.*"] border pixel 0

# Reload the configuration file
bindsym $mod+$mod2+c reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+$mod2+r restart
# Exit i3 (logs you out of your X session)
bindsym $mod+$mod2+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# Lock screen with random picture
#bindsym $mod+$mod2+l exec "perl -e '@pics = glob(\\"~/Pictures/wallpapers/*\\"); print $pics[rand @pics];' | xargs i3lock -i"
# Lock screen with current screenshot and lock image
bindsym $mod+$mod2+l exec "~/scripts/i3lock_fancy"

# Resize window (you can also use the mouse for that)
mode "resize" {
        # Pressing an arrow key will grow/shrink the window by 1px.
        bindsym h resize shrink width 1 px or 1 ppt
        bindsym j resize grow height 1 px or 1 ppt
        bindsym k resize shrink height 1 px or 1 ppt
        bindsym l resize grow width 1 px or 1 ppt
        # Shift-pressing an arrow key will grow/shrink the window by 10px.
        bindsym Shift+h resize shrink width 10 px or 10 ppt
        bindsym Shift+j resize grow height 10 px or 10 ppt
        bindsym Shift+k resize shrink height 10 px or 10 ppt
        bindsym Shift+l resize grow width 10 px or 10 ppt

        # Back to normal: Enter or Escape
        bindsym $return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Gaps between windows
gaps inner 12
gaps outer 0

mode "Inner gaps" {
    bindsym plus gaps inner all plus 1
    bindsym minus gaps inner all minus 1

    bindsym Shift+plus gaps inner all plus 5
    bindsym Shift+minus gaps inner all minus 5

    bindsym 0 gaps inner all set 0
    bindsym 1 gaps inner all set 1
    bindsym 2 gaps inner all set 2
    bindsym 3 gaps inner all set 3
    bindsym 4 gaps inner all set 4
    bindsym 5 gaps inner all set 5
    bindsym 6 gaps inner all set 6
    bindsym 7 gaps inner all set 7
    bindsym 8 gaps inner all set 8
    bindsym 9 gaps inner all set 9

    bindsym plus gaps inner current plus 1
    bindsym minus gaps inner current minus 1

    bindsym $mod2+Shift+plus gaps inner current plus 5
    bindsym $mod2+Shift+minus gaps inner current minus 5

    bindsym $mod2+0 gaps inner current set 0
    bindsym $mod2+1 gaps inner current set 1
    bindsym $mod2+2 gaps inner current set 2
    bindsym $mod2+3 gaps inner current set 3
    bindsym $mod2+4 gaps inner current set 4
    bindsym $mod2+5 gaps inner current set 5
    bindsym $mod2+6 gaps inner current set 6
    bindsym $mod2+7 gaps inner current set 7
    bindsym $mod2+8 gaps inner current set 8
    bindsym $mod2+9 gaps inner current set 9

    bindsym $return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+i mode "Inner gaps"

mode "Outer gaps" {
    bindsym plus gaps outer all plus 1
    bindsym minus gaps outer all minus 1

    bindsym Shift+plus gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5

    bindsym 0 gaps outer all set 0
    bindsym 1 gaps outer all set 1
    bindsym 2 gaps outer all set 2
    bindsym 3 gaps outer all set 3
    bindsym 4 gaps outer all set 4
    bindsym 5 gaps outer all set 5
    bindsym 6 gaps outer all set 6
    bindsym 7 gaps outer all set 7
    bindsym 8 gaps outer all set 8
    bindsym 9 gaps outer all set 9

    bindsym plus gaps outer current plus 1
    bindsym minus gaps outer current minus 1

    bindsym $mod2+Shift+plus gaps outer current plus 5
    bindsym $mod2+Shift+minus gaps outer current minus 5

    bindsym $mod2+0 gaps outer current set 0
    bindsym $mod2+1 gaps outer current set 1
    bindsym $mod2+2 gaps outer current set 2
    bindsym $mod2+3 gaps outer current set 3
    bindsym $mod2+4 gaps outer current set 4
    bindsym $mod2+5 gaps outer current set 5
    bindsym $mod2+6 gaps outer current set 6
    bindsym $mod2+7 gaps outer current set 7
    bindsym $mod2+8 gaps outer current set 8
    bindsym $mod2+9 gaps outer current set 9

    bindsym $return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+o mode "Outer gaps"

# Open a URL or search
mode "search" {
       bindsym u exec "~/scripts/dmenu_open_url"; mode "default"

       bindsym $return mode "default"
       bindsym Escape mode "default"
}

bindsym $mod+u mode "search"

# Quickly switch between windows and move them around.
mode "navigate" {
       bindsym $return mode "default"
       bindsym Escape mode "default"

       bindsym h focus left
       bindsym j focus down
       bindsym k focus up
       bindsym l focus right
       bindsym Left focus left
       bindsym Down focus down
       bindsym Up focus up
       bindsym Right focus right

       bindsym Shift+Left move left
       bindsym Shift+Down move down
       bindsym Shift+Up move up
       bindsym Shift+Right move right

       bindsym Shift+h move left
       bindsym Shift+j move down
       bindsym Shift+k move up
       bindsym Shift+l move right

       bindsym 1 workspace 1
       bindsym 2 workspace 2
       bindsym 3 workspace 3
       bindsym 4 workspace 4
       bindsym 5 workspace 5
       bindsym 6 workspace 6
       bindsym 7 workspace 7
       bindsym 8 workspace 8
       bindsym 9 workspace 9
       bindsym 0 workspace 10
       bindsym BackSpace workspace back_and_forth
       bindsym w exec "~/scripts/dmenu_i3_workspace"

       bindsym Shift+1 move container to workspace 1
       bindsym Shift+2 move container to workspace 2
       bindsym Shift+3 move container to workspace 3
       bindsym Shift+4 move container to workspace 4
       bindsym Shift+5 move container to workspace 5
       bindsym Shift+6 move container to workspace 6
       bindsym Shift+7 move container to workspace 7
       bindsym Shift+8 move container to workspace 8
       bindsym Shift+9 move container to workspace 9
       bindsym Shift+0 move container to workspace 10
       bindsym Shift+w exec "~/scripts/dmenu_i3_move_to_workspace"

       bindsym m exec "~/scripts/dmenu_i3_mark"
       bindsym g exec "~/scripts/dmenu_i3_goto"
       bindsym asciitilde exec "~/scripts/dmenu_i3_goto"

       bindsym Shift+space floating toggle
       bindsym space focus mode_toggle

       bindsym a focus parent
       bindsym z focus child

       bindsym s layout stacking
       bindsym t layout tabbed
       bindsym e layout toggle split

       bindsym q kill

       bindsym u mode "search"
}

bindsym $mod+Escape mode "navigate"
bindsym $mod+n mode "navigate"

# Set a desktop background.
# Loads a background from the current colorscheme.
exec "~/scripts/set_background"

# Start lxpanel for a topbar and dzen2 for a workspace bar.
#exec "lxpanel &"
#exec "$HOME/scripts/wsbar_start"

# Colorscheme
# Order is: bg border text indicator
# Don't edit this; rather, edit the files under ~/colorschemes/
### Autogenerated by ~/scripts/colorscheme ###
client.focused          #44c50e #44c50e #292d27 #44c50e
client.focused_inactive #4e9231 #4e9231 #292d27 #4e9231
client.unfocused        #456039 #456039 #292d27 #456039
client.urgent           #f5002b #f5002b #312a2c #f5002b
### End autogenerated section ###

# Start i3bar to display a workspace bar
# Currently I'm using lxpanel and dzen2 instead of i3bar.
bar {
        position top
        workspace_buttons yes
        status_command ~/scripts/status

        font pango:Muli 8

        colors {
               background #000000
               statusline #ffffff

               #                  border  bg      text
               focused_workspace  #000000 #000000 #ffffff
               active_workspace   #000000 #000000 #ffffff
               inactive_workspace #000000 #000000 #555555
               urgent_workspace   #000000 #000000 #ff0000
        }
}
